<div class="contenido-desk">
<div class="square-mobile">
<p class="text-center"><i class="material-icons blue10">verified_user</i></p>
<h2 class="text-center">¿Olvidaste tú contraseña?</h2><hr/>
<p class="text-center">Por favor ingrese su dirección de correo electrónico y le enviaremos las instrucciones para restablecer su contraseña.</p>
<%= simple_form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs text-center">
    <%= f.input :email, label: "Correo electrónico", autofocus: true ,placeholder:"nombredeusuario@dominio.com"%>
  </div>

  <div class="form-actions text-center">
    <%= f.button :submit, "Restablecer mi contraseña",class:"button btn-box" %>
  </div>
<% end %>
</div>
</div>